// import * as ActionTypes from './ActionTypes'
//
// export const feedback = (state={
//   feedback=[]
// }, action) => {
//   switch (action.type) {
//     case ActionTypes.ADD_FEEDBACKS:
//       var feedback = state.feedback.concat(action.payloads);
//       return {..state, feedback:feedback, errMess:null};
//     // case ActionTypes.FEEDBACKS_FAILED:
//     //   return {...state, errMess:action.payloads}
//     default:
//       return state;
//   }
// }
